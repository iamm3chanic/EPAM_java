public class PassengerPlane extends Plane {
    public PassengerPlane(PlaneType planeType, String fuelType, int fuelCount, int volume, int weightlift, int distance) {
        super(planeType, fuelType, fuelCount, volume, weightlift, distance);
    }
}
