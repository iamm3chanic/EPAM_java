package domain;

public class CargoPlane extends Plane {

    public CargoPlane(PlaneType planeType, String fuelType, int fuelCount, int volume, int weightlift, int distance) {
        super(planeType, fuelType, fuelCount, volume, weightlift, distance);
    }
}
