package domain;

public class BomberPlane extends Plane {
    public BomberPlane(PlaneType planeType, String fuelType, int fuelCount, int volume, int weightlift, int distance) {
        super(planeType, fuelType, fuelCount, volume, weightlift, distance);
    }
}

